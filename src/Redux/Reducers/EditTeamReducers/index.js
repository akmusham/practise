import * as constants from "../../Constants/EditTeamConstants"

const initialState = {
  teamid:"",
  openErrorDialog: false,
  errorDialogueMessage: ""
}

export default function editTeamOperations(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
