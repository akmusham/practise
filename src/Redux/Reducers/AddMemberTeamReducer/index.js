import * as constants from "../../Constants/AddMemberTeamConstants"

const initialState = {
  email: "",
  openErrorDialog: false,
  errorDialogueMessage: ""
}

export default function addMemTeamOperations(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
