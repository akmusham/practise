import * as constants from "../../Constants/DeleteTeamConstants"

const initialState = {
  teamid:"",
  openErrorDialog: false,
  errorDialogueMessage: ""
}

export default function deleteTeamOperations(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
